
/* Vorüberlegungen */
// push() / pop()

/*
// push --> Daten rein ... (+)
let arr = [];
ausgabe(arr);
arr.push("Ich");
ausgabe(arr);
arr.push("bin");
ausgabe(arr);
arr.push("Peter");
ausgabe(arr);

// pop() --> Daten raus ... (-)
ausgabe(arr.pop());
ausgabe(arr);
ausgabe(arr.pop());
ausgabe(arr);
ausgabe(arr.pop());
ausgabe(arr);

//ausgabe("hi");
function ausgabe(outputStr) {
    console.log(outputStr);
}
*/